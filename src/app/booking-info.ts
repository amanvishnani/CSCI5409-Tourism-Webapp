export interface BookingInfo {
    journeyId: string;
    transactionMode: string
    amount: string
    totalSeats: string
    cardNumber: string
    holderName: string
    mm: string
    yy: string
    cvv: string
}
